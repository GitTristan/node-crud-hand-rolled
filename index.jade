doctype html
html(lang="en")
  body
    h1 Welcome to the Cars CRUD App!

    h4 Here is a listing of the current cars we have:

    ul
      for car in cars
        li= car.driver + ' in a ' + car.year + ' ' + car.make + ' ' + car.model
        a(href="/cars/#{car.id}") Show

    a(href="/cars/new") Make a new car
